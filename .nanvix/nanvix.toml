[package]
name = "lxml"
version = "5.3.0"
nanvix-version = "0.12.529"

[builds]
[builds.matrix]
platforms = ["microvm"]
modes = ["multi-process", "single-process", "standalone"]
memory = ["256mb"]

[dependencies]
zlib = "1.3.1"
libxml2 = "2.12.9"
libxslt = "1.1.42"
